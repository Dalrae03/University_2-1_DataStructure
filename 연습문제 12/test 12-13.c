/*
#include <stdio.h>
#include <stdlib.h>
#define NAME_SIZE 500

typedef struct
{
	int key;
	char name[NAME_SIZE];
}record;


// »ğÀÔÁ¤·Ä

void insertion_sort(int list[], int n)
{
	int i, j, key;
	for (i = 1; i < n; i++) {
		key = list[i];
		for (j = i - 1; j >= 0 && list[j] > key; j--)
			list[j + 1] = list[j];
		list[j + 1] = key;
	}
}

int main(void)
{
	record lineup[10];





	return 0;
}
*/